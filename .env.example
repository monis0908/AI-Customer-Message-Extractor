# Create a .env file from this template and add your Gemini API key locally.
GEMINI_API_KEY=my-api-key
# Fast, cost-conscious model for structured extraction. Change it only in .env.
GEMINI_MODEL=models/gemini-3.6-flash
